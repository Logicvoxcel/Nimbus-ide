<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      No general INTERNET permission: the editor, syntax highlighting, and
      file access all run fully on-device. RUN_COMMAND below is Termux's own
      permission (not a network permission) - it lets this app ask the
      separately-installed Termux app to execute a command on our behalf.
    -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- Required on Android 11+ (targetSdk 30+) to even detect whether
         Termux is installed - without this, package visibility rules hide
         other apps from PackageManager queries by default. -->
    <queries>
        <package android:name="com.termux" />
    </queries>

    <application
        android:label="Nimbus IDE"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receives the result (stdout/stderr/exit code) back from Termux
             after it runs a command we sent it. Not exported - only Termux
             delivering to the exact PendingIntent we handed it can reach it. -->
        <service
            android:name=".TermuxResultService"
            android:exported="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>

