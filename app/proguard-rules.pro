# Add project specific ProGuard rules here.
# Nimbus IDE loads its UI from a WebView, so no JS-reflection rules are needed
# for the JavascriptInterface class beyond the @JavascriptInterface annotation,
# which the platform already keeps.
